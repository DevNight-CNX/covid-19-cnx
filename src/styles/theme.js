import { color, elementColor } from './color';
import typography, { font } from './typography';
import zIndex from './zIndex';

export default {
  typography,
  elementColor,
  color,
  zIndex,
  font
};
