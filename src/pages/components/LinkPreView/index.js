import React from 'react';
import PropTypes from 'prop-types';

import LinkPreviewView from './index.view';

const LinkPreview = () => {
  return <LinkPreviewView />;
};

LinkPreview.propTypes = {};

export default LinkPreview;
